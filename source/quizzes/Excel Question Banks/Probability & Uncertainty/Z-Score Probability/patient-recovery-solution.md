To compute the Z-score, we use the formula  

$Z = \dfrac{x - \mu}{\sigma}$,

where:
- $x$ is the observed value,
- $\mu$ is the mean,
- $\sigma$ is the standard deviation.

Substituting the given values:

Z = $\frac{18.2 - 14.4}{3}$.


This fraction is the expression used to find the Z-score (not evaluated).
